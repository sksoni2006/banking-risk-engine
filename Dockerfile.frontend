# Dockerfile.frontend
FROM node:20

WORKDIR /app

COPY frontend/package.json frontend/package-lock.json ./
RUN npm install

COPY frontend/ ./

# Expose UI port
EXPOSE 3000

# Start Vite with --host to allow browser access
CMD ["npm", "run", "dev", "--", "--host", "--port", "3000"]